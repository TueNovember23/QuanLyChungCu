<UserControl x:Class="Forms.Views.Accountant.RepairInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100"
             Background="White">
    <!-- Thêm thuộc tính Background cho UserControl -->
    <Grid Background="White">
        <!-- Đặt nền trắng cho Grid -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Lập Hóa Đơn Sửa Chữa" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="16"
                   Grid.Row="0"/>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" Margin="16">
            <StackPanel>
                <!-- Thông tin khách hàng -->
                <GroupBox Header="Thông tin khách hàng" Margin="0,0,0,16" Background="White">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Căn hộ:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,0"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding Apartments}" 
                                  SelectedValue="{Binding SelectedApartment}" 
                                  DisplayMemberPath="Name" 
                                  Margin="0,0,0,8"/>

                        <TextBlock Text="Ngày lập:" Grid.Row="1" Grid.Column="0" Margin="0,0,8,0"/>
                        <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding InvoiceDate}" Margin="0,0,0,8"/>

                        <TextBlock Text="Tên khách hàng:" Grid.Row="2" Grid.Column="0" Margin="0,0,8,0"/>
                        <Grid Grid.Row="2" Grid.Column="1">
                            <TextBox x:Name="CustomerNameTextBox" Text="{Binding CustomerName}" Margin="0,0,0,8"/>
                            <TextBlock Text="Nhập tên khách hàng..."
                                       Foreground="Gray" 
                                       IsHitTestVisible="False" 
                                       VerticalAlignment="Center" 
                                       Margin="5,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=CustomerNameTextBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=CustomerNameTextBox}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=CustomerNameTextBox}" Value=" ">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <TextBlock Text="Số điện thoại:" Grid.Row="3" Grid.Column="0" Margin="0,0,8,0"/>
                        <Grid Grid.Row="3" Grid.Column="1">
                            <TextBox x:Name="PhoneNumberTextBox" Text="{Binding PhoneNumber}" Margin="0,0,0,8"/>
                            <TextBlock Text="Nhập số điện thoại..."
                                       Foreground="Gray" 
                                       IsHitTestVisible="False" 
                                       VerticalAlignment="Center" 
                                       Margin="5,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=PhoneNumberTextBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=PhoneNumberTextBox}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=PhoneNumberTextBox}" Value=" ">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </GroupBox>

                <!-- Các phần còn lại tương tự -->
                <!-- ... -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
