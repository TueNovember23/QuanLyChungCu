<UserControl x:Class="Forms.Views.Accountant.RepairInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Forms.Views.Accountant"
             xmlns:header="clr-namespace:Forms.Views"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100">

    <Grid HorizontalAlignment="Left" Width="1100">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Grid Grid.Column="0" Background="#F6F6F8" Grid.ColumnSpan="2" Margin="0,0,1,0" Name="RenderHeader">
            <header:Header />
        </Grid>

        <!-- Danh sách hóa đơn sửa chữa -->
        <Grid Grid.Column="0" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="1" Margin="16 60 16 16" UniformCornerRadius="20">
                <StackPanel>
                    <TextBlock Text="Danh sách hóa đơn sửa chữa" FontSize="18" FontWeight="SemiBold" Margin="16"/>
                    <DataGrid x:Name="RepairInvoiceDataGrid" 
          AutoGenerateColumns="False" 
          Margin="16" 
          ItemsSource="{Binding RepairInvoices}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã HĐ" Binding="{Binding InvoiceId}" />
                            <DataGridTextColumn Header="Nội dung hóa đơn" Binding="{Binding InvoiceContent}" />
                            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount}" />
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" />
                            <DataGridTextColumn Header="Ngày lập" Binding="{Binding InvoiceDate}" />
                        </DataGrid.Columns>
                    </DataGrid>

                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Chức năng -->
        <StackPanel Grid.Column="1" Margin="16 60 16 16">
            <!-- Tạo hóa đơn sửa chữa -->
            <materialDesign:Card Margin="16" UniformCornerRadius="20">
                <StackPanel Margin="16">
                    <TextBlock Text="Lập hóa đơn sửa chữa" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Vị trí hư hỏng" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Tên thiết bị" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Mô tả hư hỏng" Margin="0,0,0,16" Height="100" 
                             TextWrapping="Wrap" AcceptsReturn="True"/>
                    <TextBox materialDesign:HintAssist.Hint="Chi phí sửa chữa" Margin="0,0,0,16"/>
                    <DatePicker materialDesign:HintAssist.Hint="Ngày sửa chữa" Margin="0,0,0,16"/>
                    <Button Content="Tạo hóa đơn" 
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Command="{Binding CreateInvoiceCommand}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Tìm kiếm hóa đơn -->
            <materialDesign:Card Margin="16" UniformCornerRadius="20">
                <StackPanel Margin="16">
                    <TextBlock Text="Tìm kiếm hóa đơn" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Nhập mã hóa đơn" Margin="0,0,0,16"/>
                    <Button Content="Tìm kiếm" 
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Command="{Binding SearchInvoiceCommand}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Thống kê -->
            <materialDesign:Card Margin="16" UniformCornerRadius="20">
                <StackPanel Margin="16">
                    <TextBlock Text="Thống kê" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBlock Text="Tổng số hóa đơn: {Binding TotalInvoiceCount}" Margin="0,0,0,16"/>
                    <TextBlock Text="Tổng chi phí: {Binding TotalRepairCost}" Margin="0,0,0,16"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>