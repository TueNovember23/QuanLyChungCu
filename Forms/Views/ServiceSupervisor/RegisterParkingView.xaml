<UserControl x:Class="Forms.Views.ServiceSupervisor.RegisterParkingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Forms.Views.ServiceSupervisor"
             xmlns:header="clr-namespace:Forms.Views"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100">

    <Grid HorizontalAlignment="Left" Width="1100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <header:Header Grid.Row="0"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="669*"/>
                <ColumnDefinition Width="431*"/>
            </Grid.ColumnDefinitions>

            <!-- Search Card -->
            <materialDesign:Card Grid.Column="0" Margin="16,16,16,0" UniformCornerRadius="20" Height="162" VerticalAlignment="Top">
                <StackPanel Margin="16">
                    <TextBlock Text="Tìm kiếm căn hộ" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Nhập mã căn hộ hoặc tên chủ hộ" Margin="0,0,0,16" Text="{Binding SearchText}"/>
                    <Button Content="Tìm kiếm" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,0,16" Command="{Binding SearchCommand}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Space between Search and Apartment List -->
            <Grid Grid.Column="0" Margin="0,380,0,380"/>

            <!-- Apartment List -->
            <materialDesign:Card Grid.Column="0" Margin="16,192,16,16" UniformCornerRadius="20">
                <StackPanel Margin="16,16,16,16">
                    <TextBlock Text="Danh sách căn hộ" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <ListView x:Name="ApartmentListView" ItemsSource="{Binding Apartments}" SelectedItem="{Binding SelectedApartment}" SelectionMode="Single">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Mã căn hộ" DisplayMemberBinding="{Binding ApartmentCode}" Width="100"/>
                                <GridViewColumn Header="Diện tích (m²)" DisplayMemberBinding="{Binding Area}" Width="150"/>
                                <GridViewColumn Header="Trạng thái" DisplayMemberBinding="{Binding Status}" Width="150"/>
                                <GridViewColumn Header="Tầng" DisplayMemberBinding="{Binding FloorNumber}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </materialDesign:Card>

            <!-- Thông tin xe và chủ xe -->
            <materialDesign:Card Grid.Column="1" Margin="16,16,16,0" UniformCornerRadius="20" Height="323" VerticalAlignment="Top">
                <StackPanel Margin="16">
                    <TextBlock Text="Thông tin xe và chủ xe" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Biển số xe" Margin="0,0,0,16" Text="{Binding Vehicle.VehicleNumber}"/>
                    <TextBox materialDesign:HintAssist.Hint="Loại xe" Margin="0,0,0,16" Text="{Binding Vehicle.VehicleType}"/>
                    <TextBox materialDesign:HintAssist.Hint="Tên chủ xe" Margin="0,0,0,16" Text="{Binding Vehicle.OwnerName}"/>
                    <TextBox materialDesign:HintAssist.Hint="Số điện thoại chủ xe" Margin="0,0,0,16" Text="{Binding Vehicle.OwnerPhone}"/>
                    <Button Content="Đăng ký thông tin xe" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding CheckInfoCommand}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Space between Thông tin xe và chủ xe and Thông tin thanh toán -->
            <Grid Grid.Column="1" Margin="0,0,0,16"/>

            <!--Thông tin thanh toán-->
            <materialDesign:Card Grid.Column="1" Margin="16,361,16,16" UniformCornerRadius="20">
                <StackPanel Margin="16">
                    <TextBlock Text="Thông tin thanh toán" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBox materialDesign:HintAssist.Hint="Số tiền thanh toán" Margin="0,0,0,16" Text="{Binding PaymentAmount}"/>
                    <ComboBox materialDesign:HintAssist.Hint="Phương thức thanh toán" Margin="0,0,0,16" ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod}"/>
                    <ComboBox materialDesign:HintAssist.Hint="Trạng thái thanh toán" Margin="0,0,0,16" ItemsSource="{Binding PaymentStatuses}" SelectedItem="{Binding SelectedPaymentStatus}"/>
                    <Button Content="In thẻ xe" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,0,16" Command="{Binding SaveAndPrintCommand}"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
